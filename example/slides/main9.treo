section reo.slides;

import reo.sync;
import reo.shiftlossyfifo1;

import reo.slides.red;
import reo.slides.green;
import reo.slides.blue;
import reo.slides.seqc;

main9() {
  green(a) red(b) blue(c) connector9(a, b, c)
}

connector9(a, b, c) {
  seqc(x, y) sync(a, x) sync(b, y) 
  shiftlossyfifo1(m, c) sync(x, m) sync(y, m)
}
