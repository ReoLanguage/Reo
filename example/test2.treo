import reo.sync;
import reo.lossy;
import reo.syncdrain;
import reo.fifo1;

//test2(a?String,b?String,c!String){
//test2(a?String,b?String,c?String,d?String){

test2(a?String,c?String,b!String){

	sync(a,x)
	lossy(b,x)
	syncdrain(x,y)
	sync(c,y)
	sync(d,y)
//	sync(a,b)
//	syncdrain(a,c)
//	sync(c,d)
//	fifo1(d,b)
}
