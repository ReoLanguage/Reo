import reo.fifo1;
import reo.sync;

producer(a!String) {
	#RBA {a} true
	Java:"Workers2.producer"
}

consumer(a?String) {
	#RBA {a} true
	Java:"Workers2.consumer"
}


earlyAsyncReplicator(){
	fifo1(c,b)
	producer(c)
{
	{ sync(b,a[i]) consumer(a[i]) | i:<1..(N+1)> }
|
 	N=3
}
}
