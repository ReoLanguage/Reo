import reo.sequencer;
import reo.wa.fifo1;
import reo.wa.fifofull;
import reo.wa.syncdrain;
//import reo.wa.sequencer;

test() {

sequencer = <k>(a[1..k]) { reo.sequencer<k, fifo1, fifofull, syncdrain>(a[1..k]) }

	//sequencer<2>(a,b)


  //fifo2 = (a?,b!) { fifo1(a,x) fifo1(x,b) }
	
	//(a,b) { fifo2(a,y) fifo2(y,b) } (s,t)

	//n = 3
	
	//fifo[0] = (a?,b!){ a = b }

	//for k = 1..n {
	//	fifo[k] = (a?,b!) { fifo1(a,d) fifo[k-1](d,b) }
	//}
	
	//fifo[n](s,t)
}
