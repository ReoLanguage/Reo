import reo.wa.fifo1;

test() {

  fifo2 = (a?,b!) { fifo1(a,x) fifo1(x,b) }
	
	(a,b) { fifo2(a,y) fifo2(y,b) } (s,t)

	//n = 3
	
	//fifo[0] = (a?,b!){ a = b }

	//for k = 1..n {
	//	fifo[k] = (a?,b!) { fifo1(a,d) fifo[k-1](d,b) }
	//}
	
	//fifo[n](s,t)
}
