section reo.slides;

import reo.fifo1;
import reo.fifo1full;
import reo.sync;

seqp(p[1..n]) {
  {
    fifo1(x[i], x[i+1])
    sync(x[i+1], p[i])
  | 
    i : <1..n>, i != k 
  }
  fifo1full<0>(x[k], x[k+1])
  sync(x[n+1], x[1])
|
  k = 1
}












