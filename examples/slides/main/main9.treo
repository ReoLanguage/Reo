section slides.main;

import reo.sync;
import slides.shiftlossyfifo.shiftlossyfifo;


import slides.producer.red;
import slides.producer.green;
import slides.consumer.blue;
import slides.sequencer.seqc;

main9() {
  green(a) red(b) blue(c) connector9(a, b, c)
}

connector9(a, b, c) {
  seqc(x, y) sync(a, x) sync(b, y) 
  shiftlossyfifo(m, c) sync(x, m) sync(y, m)
}
