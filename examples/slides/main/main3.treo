section slides.main;

import reo.sync;
import reo.lossy;

import slides.producer.red;
import slides.producer.green;
import slides.consumer.blue;
import slides.sequencer.seqc;

main3() {
  green(a) red(b) blue(c) connector3(a, b, c)
}

connector3(a, b, c) {
  seqc(x, y) lossy(a, x) lossy(b, y) 
  sync(m, c) sync(x, m) sync(y, m)
}




