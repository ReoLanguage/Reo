// The compiler does not support sync/async partitioning with this example.
// Compile this file with the option "-pt"

section slides.main;

import reo.sync;
import slides.shiftlossyfifo.shiftlossyfifo;

import slides.producer.red;
import slides.producer.green;
import slides.consumer.blue;
import slides.sequencer.seqc;

main7() {
  green(a) red(b) blue(c) connector7(a, b, c)
}

connector7(a, b, c) {
  seqc(x, y) shiftlossyfifo(a, x) shiftlossyfifo(b, y) 
  sync(m, c) sync(x, m) sync(y, m)
}
