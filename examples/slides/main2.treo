section reo.slides;

import reo.sync;

import reo.slides.red;
import reo.slides.green;
import reo.slides.blue;
import reo.slides.seqc;

main2() {
  green(a) red(b) blue(c) connector2(a, b, c)
}

connector2(a, b, c) {
  seqc(x, y) sync(a, x) sync(b, y) 
  sync(m, c) sync(x, m) sync(y, m)
}
