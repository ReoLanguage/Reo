/**
 * An automaton is a port automaton whose transitions have, besides a synchronization 
 * constraint, a generic label that allows for easy implementation of constraint automata,
 * work automata, seepage automata, etc., without reimplementing the their composition. 
 */
package nl.cwi.reo.semantics.automata;