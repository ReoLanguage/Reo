import reo.fifo1;
import reo.sync;
import reo.syncdrain;

producer(a!) {
	#PR identity(;a)
	Java:"Workers1.producer"
}

consumer(a?) {
	#PR identity(;a)
	Java:"Workers1.consumer"
}

alternator() {
	{ syncdrain(a[i], a[i+1]) | i : <1..10> }
	{ fifo1(b[i+1], b[i]) | i : <1..10> }
	{ sync(a[i], b[i]) | i : <1..11> }
	{ producer(a[i]) | i : <1..11> }
	consumer(b[1])
}
