station<location:String>(getLoc!,charge!){
	#RBA
	$m = location;
	
	charge!=*, charge = b(10), getLoc = *
	charge = *, getLoc = $m, $m!=*, getLoc !=*
	charge!=*, charge= b(10), getLoc !=*, getLoc = $m, $m = $m'	
}