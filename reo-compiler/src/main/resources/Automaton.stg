run(A) ::= <<


>>

transition(t) ::= <<
if (<guard(t)>) {
  bla
}
>>

action(t) ::= <<

>>

doSync(p) ::= <<
<p.name>.<if(p.input)>canGet()<else>canPut()<endif>
>>

guard(t) ::= <<
<t.SyncConstraint: canSync(); separator=" && ">
>>

canSync(p) ::= <<
<p.name>.<if(p.input)>canGet()<else>canPut()<endif>
>>