import reo.fifo1;

fifon() {
  prod(a[0])
  cons(a[N])

	{
	  fifo1(a[i],a[i+1])
	|
	  i:<0..(N-1)>
	}
  |
    N = 50
}

prod(a!) {
  #PR identity(a;)
|
  Java:"Processes.prod"
}

cons(a?) {
  #PR identity(;a)
|
  Java:"Processes.cons"
}
